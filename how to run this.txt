requriments:
   run in the Terminal
	     npm install mongodb express mongoose cors

	mongodb atlas(globle host) or compass(local host) is require with connection url 




script to run:
	node <'relative path of script.js     ||file'>::run only script.js all the program will works
